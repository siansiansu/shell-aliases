gcpAddrList() {
    gcloud compute addresses list --project="$1"
}

gcpComputeList() {
    gcloud compute instances list --project="$1"
}

gcpRouteList() {
    gcloud compute routes list --project="$1"
}

gcpContainerList() {
    gcloud container clusters list --project="$1"
}
